# Generated deliverables ARE tracked in git now, so `@claude` runs triggered
# from GitHub (issues/PRs) have somewhere persistent to save reports instead
# of them vanishing when the job finishes. Only OS/editor junk is excluded.
.DS_Store
Thumbs.db
